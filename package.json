{
  "name": "dsh-effort-ultra",
  "version": "0.2.0",
  "description": "DeepSeek Harness composer 的原生推理强度控件：Codex 风格蓝紫渐变界面、连续拖动、模型切换与六档 Step Agent 菜单；档位来自官方 reasoning 声明。 / A native DSH composer reasoning-effort control with a Codex-style blue-to-violet UI, continuous dragging, model switching and a six-tier Step Agent menu driven by the official reasoning declaration.",
  "license": "MIT",
  "author": "LoveIrishCoffee",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LoveIrishCoffee/dsh-effort-ultra.git"
  },
  "homepage": "https://github.com/LoveIrishCoffee/dsh-effort-ultra#readme",
  "bugs": {
    "url": "https://github.com/LoveIrishCoffee/dsh-effort-ultra/issues"
  },
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "lib",
    "cordis.patch.yml",
    "README.md",
    "screenshots.json"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-ui-slots",
        "@deepseek-ai/dsh-client-locale"
      ],
      "platform": "web"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/smoke-client.mjs && node scripts/check-package-contract.mjs",
    "check:dist": "npm run build && git diff --exit-code -- dist",
    "prepublishOnly": "npm run build && npm test"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.2",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  },
  "keywords": [
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "reasoning-effort",
    "thinking-effort",
    "composer",
    "web-ui"
  ]
}
